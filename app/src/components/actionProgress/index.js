import angular from 'angular';

import ActionProgress from './actionProgress';

export default angular.module('fitnation.components.action-progress', [])
  .component('actionProgress', ActionProgress);
