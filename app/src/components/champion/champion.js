import templateUrl from './champion.html';
import './champion.scss';

const Champion = {
  bindings: {
    championId: '<',
    mirror: '@',
    fuckedUp: '@'
  },
  templateUrl
};

export default Champion;
